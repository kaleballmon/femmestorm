# Femme Storm
Shereen's Site

## Installation
The full site is in the `femme-storm` subdirectory. When developing, first change directories to the `femme-storm` directory.

`cd femme-storm/`

To install the depedencies, run `yarn install -D`.

> [!NOTE]
> `node >=22.12.0` is required. 
